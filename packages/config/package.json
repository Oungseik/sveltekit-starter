{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "@repo/config",
  "version": "0.1.0",
  "private": true,
  "description": "Domain logic enums and variables to share between different packages",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "check": "oxlint .",
    "build": "tsdown",
    "check-types": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "^1.4.1",
    "tsdown": "0.23.0",
    "typescript": "^7.0.2"
  }
}
